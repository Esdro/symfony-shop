{% extends 'base.html.twig' %}

{% block title %}New Address
{% endblock %}

{% block body %}
	{{ include("partials/section-title.html.twig",{'nom':' Nouvel address  '}) }}
	<div class="container">
		<div class='row'>
			<div class="col-md-6 m-auto">
				<h1>Create new Address</h1>

				{{ include('address/_form.html.twig') }}

				<a href="{{ path('app_address_index') }}">back to list</a>

			</div>
		</div>
	</div>
{% endblock %}
